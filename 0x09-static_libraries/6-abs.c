#include "main.h"
/**
 * _abs - calculates an integer's absolute value
 * @n: the int to examine
 * Return: the int value in its entirety
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
